Use `Separator` to divide content.

```jsx
<Separator />
<Separator orientation="vertical" style={{ height: 24 }} />
```

Vertical separators need an explicit height from the parent (e.g. a flex row) or inline style.
