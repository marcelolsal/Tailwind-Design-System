import * as React from "react";

/** Keyboard key hint, in the mono type family. */
export interface KbdProps extends React.HTMLAttributes<HTMLElement> {}

export function Kbd(props: KbdProps): React.JSX.Element;
