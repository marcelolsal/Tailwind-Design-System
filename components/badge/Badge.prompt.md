Use `Badge` for compact static labels — counts, categories, metadata.

```jsx
<Badge variant="success">In stock</Badge>
```

Variants: `primary` (default), `secondary`, `outline`, `success`, `warning`, `error`.
For live/dynamic state (with a colored dot) use `Status` instead.
