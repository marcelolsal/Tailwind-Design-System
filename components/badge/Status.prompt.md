Use `Status` for live state that changes — a pill with a leading indicator dot.

```jsx
<Status variant="success">Published</Status>
```

Variants: `default`, `primary`, `success`, `warning`, `error`. The dot (`StatusIndicator`) renders automatically. For static labels use `Badge`.
