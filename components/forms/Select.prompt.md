Use `Select` for choosing one option from a short list.

```jsx
<Select defaultValue="usd">
  <option value="usd">USD $</option>
  <option value="eur">EUR €</option>
</Select>
```

Native select styled to match `Input` (32px, chevron affordance).
