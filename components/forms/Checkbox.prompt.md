Use `Checkbox` for a binary opt-in inside a list or form.

```jsx
<label><Checkbox defaultChecked /> Show on storefront</label>
```

Native checkbox — controlled (`checked`+`onChange`) or uncontrolled (`defaultChecked`). Set `invalid` for the error border. For a single on/off setting toggle, prefer `Switch`.
